services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gaba
      POSTGRES_PASSWORD: gaba
      POSTGRES_DB: gaba
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

